
# Zap-z.ru - Интернет-магазин автозапчастей

Это современный, быстрый и доступный интернет-магазин автозапчастей, созданный с помощью Google Gemini. Платформа включает в себя каталог товаров, корзину, личный кабинет пользователя и панель администратора для управления контентом.

## 🚀 Технологический стек

*   **Фронтенд:** React, TypeScript, Tailwind CSS, Vite
*   **Уведомления:** Telegram-бот на Node.js (Telegraf, Express)
*   **Бэкенд (API):** Заглушка (mock) для демонстрации. Готов к интеграции с полноценным бэкендом.

---

## 🏁 Как запустить проект локально

Чтобы запустить сайт и бота на вашем компьютере, вам понадобятся [Node.js](https://nodejs.org/) (версия 18 или выше) и `npm`.

### Шаг 1: Клонирование репозитория

Сначала скопируйте проект с GitHub на ваш компьютер. Откройте терминал и выполните команду:

```bash
git clone https://github.com/voazap-wq/zap-z-ru.git
cd zap-z-ru
```

### Шаг 2: Запуск основного сайта (Фронтенд)

1.  **Установка зависимостей:**
    В терминале, находясь в корневой папке `zap-z-ru`, выполните:
    ```bash
    npm install
    ```

2.  **Запуск сервера для разработки:**
    ```bash
    npm run dev
    ```
    После этого сайт станет доступен в вашем браузере по адресу, указанному в терминале (обычно `http://localhost:5173`).

### Шаг 3: Настройка и запуск Telegram-бота (Бэкенд)

Бот нужен для отправки уведомлений о заказах. Он запускается отдельно.

1.  **Перейдите в папку бота:**
    Откройте **новый терминал** и перейдите в директорию `bot`:
    ```bash
    cd bot
    ```

2.  **Установка зависимостей бота:**
    ```bash
    npm install
    ```

3.  **Создание файла конфигурации `.env`:**
    Создайте файл `.env` в папке `bot/` и скопируйте в него содержимое файла `.env.example` из той же папки. Затем заполните его своими данными:

    ```env
    # Токен вашего бота, полученный от @BotFather в Telegram
    TELEGRAM_BOT_TOKEN=...

    # ID вашего Telegram-канала для новостей (необязательно)
    TELEGRAM_CHANNEL_ID=...

    # ID вашего Telegram-аккаунта для админ-команд. Узнайте у @userinfobot
    ADMIN_IDS=...

    # URL сайта для ссылок в уведомлениях
    BASE_URL=http://localhost:5173

    # Порт для API бота
    PORT=3001
    ```

4.  **Запуск бота:**
    В терминале (в папке `bot/`) выполните:
    ```bash
    npm start
    ```

🎉 **Готово!** Теперь у вас запущен и сайт, и бот. Вы можете регистрироваться, добавлять товары в корзину и оформлять заказы. При смене статуса заказа в админ-панели бот будет присылать уведомления в Telegram (если вы привязали свой аккаунт).

## 🌟 Основные возможности

*   **Современный UI:** Чистый, адаптивный дизайн в стиле Material Design.
*   **Каталог:** Поиск, фильтрация и сортировка товаров.
*   **Личный кабинет:** Профиль, история заказов, "Мой гараж" для сохранения авто.
*   **Админ-панель:** Управление товарами, категориями, пользователями, заказами и контентом сайта.
*   **Интеграция с Telegram:** Уведомления о статусе заказа и новостные рассылки.